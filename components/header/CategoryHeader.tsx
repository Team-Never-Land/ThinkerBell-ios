import React from "react";
import { View, SafeAreaView, Text } from "react-native";

export default function CategoryHeader({ navigation }: { navigation: any }) {
  return (
    <SafeAreaView>
      <View>
        <Text>Header</Text>
      </View>
    </SafeAreaView>
  );
}
